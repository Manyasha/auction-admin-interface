import { EventsTableFilterPipe } from './events-table-filter.pipe';

describe('EventsTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
